# Backend Environment Variables Template
# Copy these to Render Dashboard → Environment Variables

# Server Configuration
NODE_ENV=production
PORT=10000

# Firebase Admin SDK Configuration
# Get from Firebase Console → Project Settings → Service Accounts
# Click "Generate New Private Key" to download JSON file
FIREBASE_PROJECT_ID=your_project_id
FIREBASE_CLIENT_EMAIL=firebase-adminsdk-xxxxx@your-project.iam.gserviceaccount.com
FIREBASE_PRIVATE_KEY="-----BEGIN PRIVATE KEY-----\nYour_Private_Key_Here\n-----END PRIVATE KEY-----\n"

# CORS Origins (Update with your Vercel domain)
ALLOWED_ORIGINS=https://your-app.vercel.app,http://localhost:5173

# Instructions:
# 1. Go to Firebase Console → Project Settings → Service Accounts
# 2. Click "Generate New Private Key" button
# 3. Download the JSON file
# 4. Copy values from JSON:
#    - project_id → FIREBASE_PROJECT_ID
#    - client_email → FIREBASE_CLIENT_EMAIL
#    - private_key → FIREBASE_PRIVATE_KEY (keep the \n characters!)
# 5. Add these in Render Dashboard → Environment tab
# 6. For ALLOWED_ORIGINS: Add your Vercel domain after deployment
